import type { MetadataRoute } from "next"; import { blogPosts, products } from "@/lib/data"; export default function sitemap():MetadataRoute.Sitemap{const base="https://www.example.com";return ["","/products","/custom-solutions","/applications","/about","/quality-control","/blog","/contact",...products.map(p=>`/products/${p.slug}`),...blogPosts.map(p=>`/blog/${p.slug}`)].map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:"monthly",priority:url===""?1:0.7}))}

import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { products } from "@/lib/data"; import { ArrowLink, Placeholder, SectionTitle } from "@/components/ui";
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))} export function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{return params.then(({slug})=>{const p=products.find(x=>x.slug===slug);return {title:p?.name??"Product",description:p?.description}})}
export default async function ProductDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <main><section className="page-hero"><p className="eyebrow">Example product data</p><h1>{p.name}</h1><p>{p.description}</p></section><section className="section"><div className="detail-grid"><Placeholder className="detail-image" label={`${p.name} high-resolution product photography`} /><div><h2>Configuration overview</h2><table className="table"><tbody>{[["Screen size",p.size],["Resolution",p.resolution],["Brightness",p.brightness],["Interface",p.interface],["Product category",p.category],["Data status","Example data — replace before publishing"]].map(([a,b])=><tr key={a}><td>{a}</td><td>{b}</td></tr>)}</tbody></table><div className="hero-actions"><Link className="button" href="/contact">Get a Quote</Link><a className="button" href="mailto:[Company email]?subject=Specification%20Sheet%20Request">Download Specification Sheet</a></div></div></div></section><section className="section section-alt"><div className="grid grid-3"><div><SectionTitle eyebrow="Key features" title="Specified around the application"/><p>Display format, brightness, electronics and enclosure can be discussed against the real installation environment.</p></div><div className="card"><h3>Available interfaces</h3><p>HDMI, LVDS, eDP and other interface options can be evaluated for project compatibility.</p></div><div className="card"><h3>Customization items</h3><p>Screen size, resolution, brightness, touch, housing, operating system, mounting, logo and packaging.</p></div></div></section><section className="section"><SectionTitle eyebrow="Application fit" title="From retail fixture to integrated equipment"/><div className="grid grid-3">{["Retail shelf communication","Digital signage and wayfinding","Industrial or commercial equipment"].map(x=><div className="card" key={x}><h3>{x}</h3><p>Tell us about your use case so technical choices can be matched to it.</p><ArrowLink href="/contact">Discuss this application</ArrowLink></div>)}</div></section><section className="section section-alt"><SectionTitle eyebrow="Related example products" title="Explore other formats"/><div className="grid grid-3">{products.filter(x=>x.slug!==p.slug).slice(0,3).map(x=><div className="card" key={x.slug}><h3>{x.name}</h3><p>{x.size} · {x.resolution}</p><ArrowLink href={`/products/${x.slug}`}>View Details</ArrowLink></div>)}</div></section></main>}

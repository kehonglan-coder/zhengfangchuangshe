import type { Metadata } from "next"; import { products } from "@/lib/data"; import { ArrowLink, Placeholder } from "@/components/ui";
export const metadata: Metadata={title:"Stretched Bar LCD Display Products",description:"Browse example stretched bar LCD, shelf edge, ultra-wide and custom display configurations."};
export default function Products(){return <main><section className="page-hero"><p className="eyebrow">Product catalogue</p><h1>Stretched Bar LCD Display Products</h1><p>Example display configurations for initial project discussion. Product data is clearly marked for replacement with your approved specifications.</p></section><section className="section"><div className="filters">{["All products","Stretched Bar LCD Displays","Shelf Edge Displays","Ultra-wide LCD Displays","Industrial Bar Displays","Touch Bar Displays","High Brightness Displays","Custom Display Solutions"].map(x=><span className="filter" key={x}>{x}</span>)}</div><div className="grid grid-3">{products.map(p=><article className="card product-card" key={p.slug}><Placeholder label={`${p.name} product image`} /><div><p className="eyebrow">Example data · {p.category}</p><h2>{p.name}</h2><div className="specs"><span>Size: {p.size}</span><span>Resolution: {p.resolution}</span><span>Brightness: {p.brightness}</span><span>Interface: {p.interface}</span></div><p>{p.description}</p><ArrowLink href={`/products/${p.slug}`}>View Details</ArrowLink></div></article>)}</div></section></main>}
